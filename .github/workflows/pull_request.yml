name: 👾 Pull Request

on:
  pull_request:

jobs:
  lint:
    name: Lint
    uses: ./.github/workflows/lint.yml

  test:
    needs: lint
    name: Test
    uses: ./.github/workflows/test.yml

  build:
    needs: test
    name: Build
    uses: ./.github/workflows/build.yml
